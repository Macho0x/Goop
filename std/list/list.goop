module list

// Prelude List.filter / List.map / List.fold / List.find lower to Go generics.
// These names re-export the same helpers so `import goop . "std.list"` still builds.

let Filter f xs = List.filter f xs
let Map f xs = List.map f xs
let Fold f acc xs = List.fold f acc xs
