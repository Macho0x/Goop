module main

import go "strings" {
  val ToUpper : string -> string
}

@[go] {
  func greet(name string) string {
    return "Hello, " + name + "!"
  }
}
val greet : string -> string

let main () =
  let chk = assert (ToUpper "hi" = "HI") in
  println (greet "Goop")
