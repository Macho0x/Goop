module main

@[c] {
  int mul(int a, int b) { return a * b; }
}
val mul : int -> int -> int

@[c] {
  int neg(int x) { return -x; }
}
val neg : int -> int

let main () =
  let chk1 = assert (mul 3 4 = 12) in
  let chk2 = assert (neg 7 = -7) in
  println "c_embed_multi ok"
