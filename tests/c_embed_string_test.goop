module main

@[c] {
  #include <string.h>
  int goop_strlen(const char *s) {
    return (int)strlen(s);
  }
}
val goop_strlen : string -> int

let main () =
  let chk = assert (goop_strlen "hello" = 5) in
  println "c_embed_string ok"
