// Record field @[json] / @[tag "…"] lowers to Go struct tags.
module main

type wire_meta = {
  name : string @[json];
  sz   : int @[json omitempty];
  user_id : string @[json "userID"];
}

let sample : wire_meta = { name = "ETH"; sz = 1; user_id = "u1" }

let main () =
  println sample.name
