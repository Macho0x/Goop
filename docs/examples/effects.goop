module EffectsDemo

// Minimal OCaml 5-style effect demo (CPS-lowered).

effect Flip : unit -> bool

let coin () : bool =
  begin
    perform (Flip ());
    false
  end

let run () : bool =
  match coin () with
  | effect (Flip _) k -> k true
  | v -> v

let main () =
  assert (run () = true)
