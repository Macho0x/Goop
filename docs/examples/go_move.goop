module main

// go (move x) transfers a ref into the goroutine,
// suppressing data-race warnings when the parent no longer uses x.

let launch () : unit =
  let counter = ref 0 in
  let dummy = go (move counter) (fun () -> counter := !counter + 1) in
  ()

let main () =
  launch ()
