(* Minimal HTTP surface — typed import (no live server). *)
module main

import go "net/http" {
  val CanonicalHeaderKey : string -> string
}

let main () =
  begin
    let key = http.CanonicalHeaderKey "content-type" in
    println key;
    let _ = assert (key = "Content-Type") in
    ()
  end
